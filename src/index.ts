// src/player/index.ts

// Step 1: Export all types from the types file as named exports.
export * from './types';

// Step 2: Import the Player component.
import Player from './Player';

// Step 3: Export the Player component as the default export.
export default Player;
